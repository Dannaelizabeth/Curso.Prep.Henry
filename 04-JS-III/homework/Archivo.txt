Los Arrays: Las matrices como contenedores de almacenamiento para colecciones de datos. Podemos contruir una matriz es simple,
declarar una variable y establecerla en []. Luego podemos agregar al contenedor (separadas en coma).
tanta cadenas, numeros booleanos como queramos y acceder a esos elementos cuando lo deseamos.